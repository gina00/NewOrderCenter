const subMenuManage = {
    state: {
        select: "1"
    },
    mutations: {
        changeSubMenu(state, select) {
            state.select = select;
        }
    }
};
export default subMenuManage